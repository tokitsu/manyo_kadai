<h3>一覧</h3>
<%= notice %><br>
<% @tasks.each do |task| %>
名前<%= task.name %><br>
内容<%= task.content %><br>
<%= link_to "編集",edit_task_path(task.id) %>
<%= link_to "詳細", task_path(task.id) %>
<%= link_to "削除", task_path(task.id), method: :delete, data:{ confirm: "本当に削除していいですか？"} %><br>
<% end %><br><br>
<%= link_to "新規登録", new_task_path %>
